load these zip files into arduino to run
if not running same setup, will have to modify user setup .h for eSPI TFT screen

In Arduino, in top menu:
Sketch > Include Library > Add .ZIP Library
-Include each library in this folder